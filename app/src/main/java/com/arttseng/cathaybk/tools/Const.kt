package com.arttseng.cathaybk.tools

class Const {
    companion object {
        var BaseURL = "https://api.github.com/"
        const val GetAllUsers = "/users"
        const val GetUserDetail = "/users/{name}"
    }
}